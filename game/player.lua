function createPlayer()
    player = {}
    player.w = 40
    player.h = 40
    player.x = (windowWidth / 2) - player.w
    player.y = (windowHeight / 2) - player.h
    player.speed = 180
end

function drawPlayer()

end
